ignore_checks:
  - W3045 # W3045 Consider using AWS::S3::BucketPolicy instead of AccessControl
  - E3001 # CloudFormation can use tags on redshift resources
  - W1028 # W1028 Fn::If has a path that cannot be reached (false positive on mutually exclusive conditions)
